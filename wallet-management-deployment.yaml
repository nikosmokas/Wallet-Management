apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-management-deployment
  namespace: wallet-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wallet-management
  template:
    metadata:
      labels:
        app: wallet-management
    spec:
      containers:
        - name: wallet-management
          image: wallet-management-app:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql-service:3306/yourdb"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "root123"
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1"

---
apiVersion: v1
kind: Service
metadata:
  name: wallet-management-service
  namespace: wallet-management
spec:
  ports:
    - port: 8080
  selector:
    app: wallet-management
